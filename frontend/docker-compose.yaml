version: "3.3"

services:
  frontend:
    build:
      context: https://github.com/phhphc/nft-marketplace-front-end.git#dev
      args:
        - BACKEND_URL=$BACKEND_URL
        - NEXT_PUBLIC_MKP_ADDRESS=$NEXT_PUBLIC_MKP_ADDRESS
        - NEXT_PUBLIC_ERC721_ADDRESS=$NEXT_PUBLIC_ERC721_ADDRESS
    ports:
      - 8080:8080
